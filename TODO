* unit tests (?)
* "sandbox" process which starts the transport and restarts if it disconnects or crashes.
* Gateway: exchanging contacts via XEP-0114 (Roster Item Exchange)
* Setting/retrieving icq away/na status messages (?)
* file transfer (?)
